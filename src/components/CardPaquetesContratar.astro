---
interface Props {
  title: string;
  mbps: string;
  price: string;
  desc: string;
}

const { mbps, title, price, desc } = Astro.props;
---

<div class=" justify-between flex flex-col w-full  lg:w-1/4 xl:w-1/5  bg-white bg-opacity-0 backdrop-blur-sm border-2 rounded-lg border-white-300 px-4 py-1 lg:px-2 ">
    <div class="flex flex-row lg:flex-col justify-between items-center">
        <h2 class=" text-base sm:text-xl text-start lg:text-center">{title}</h2>
        <p class="text-lg lg:text-center flex lg:hidden px-3">{mbps}</p>
        <p class="text-lg lg:hidden">${price}</p>
    </div>
    <p class="text-base text-center hidden lg:flex">{desc}</p>
    <p class="text-lg text-start hidden lg:hidden">{mbps}</p>
    <p class="text-lg hidden lg:block text-end ">${price}</p>
</div>

